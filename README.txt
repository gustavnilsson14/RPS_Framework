The RPS_Framework is an attempt to reuse some lessons learned from MVC frameworks for web applications, and apply them to Unity.

The framework allows for separation of game logic, and rendering, while also allowing for easy rerendering of any view objects on an event basis.